# k-means-clustering
K-means clustering is a popular unsupervised machine learning algorithm used to group unlabeled data points into k distinct groups based on similarity. It relies on an iterative process of setting center points, assigning data, and updating means.
